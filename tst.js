

{/*sites.map(e=>
    valor = e,
    valorUpdated = valor.replace('https://',''),
    hosts = [valorUpdated],
    hosts.forEach(function (host) {
    ping.promise.probe(host)
        .then(function (res) {
          e = res.numeric_host
        });        
    }),
    console.log(ip),
    ips.push(ip)
  )*/}
{/*}
  
    console.log(ips)
 //*/}